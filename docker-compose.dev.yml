version: '3.8'

# Development override - chỉ chạy database services
# Sử dụng: docker-compose -f docker-compose.yml -f docker-compose.dev.yml up -d

services:
  mongodb:
    ports:
      - '27017:27017'

  mongo-express:
    ports:
      - '8081:8081'

  redis:
    ports:
      - '6379:6379'
